import { createTheme } from "@mui/material";

const theme = createTheme({
  cssVariables: true,
  palette: {
    primary: {
      main: "#3f51b5",
    },
    secondary: {
      main: "#f50057",
    },
  },
});

export default theme;
